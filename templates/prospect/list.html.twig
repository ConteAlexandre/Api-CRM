{% extends 'base.html.twig' %}

{% block title %}List Prospects{% endblock title %}

{% block body %}
    <table class="table">
        <thead>
        <tr>
            <th scope="col">Prenom</th>
            <th scope="col">Nom</th>
            <th scope="col">Email</th>
            <th scope="col">Activité</th>
            <th scope="col">Modifier</th>
            <th scope="col">Profil</th>
        </tr>
        </thead>
        {% for prospect in prospects %}
            <tbody>
            <tr>
                <td>{{ prospect.firstName }}</a></td>
                <td>{{ prospect.lastName }}</td>
                <td>{{ prospect.email }}</td>
                <td>{{ prospect.clientActivity }}</td>
                <td><a href="{{ path('update_client', {'slug': prospect.slug }) }}">Modifier</a></td>
                <td><a href="{{ path('profileClient', {'slug': prospect.slug}) }}">Profil</a></td>
            </tr>
            </tbody>
        {% endfor %}
    </table>
{% endblock body %}